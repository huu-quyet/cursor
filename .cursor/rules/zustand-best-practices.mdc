---
description: 
globs: **/*.ts,**/*.tsx
alwaysApply: false
---
---
description: Best practices for state management with Zustand
globs: **/*.ts,**/*.tsx
---

- Use `create` to define your store for better organization
- Implement middleware like `persist` for state persistence
- Use `shallow` comparison to optimize re-renders
- Keep stores focused on specific domains for better maintainability
- Utilize `immer` middleware for easier state updates